using UnityEngine;

public class TaxLand : Land
{
    /*private TaxLandAction _taxLandAction;

    protected override void Start()
    {
        base.Start();
        _taxLandAction = LandActionFactory.Instance.GetLandAction(LandAction.Type.Tax) as TaxLandAction;
    }
    protected override void DisplayLandAction(Player arrivedPlayer)
    {
        base.DisplayLandAction(arrivedPlayer);
        Debug.Log(arrivedPlayer.name + " arrived at Tax Land");
        _taxLandAction.Invoke(this);
    }*/
}
